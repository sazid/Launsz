<resources>

    <!-- just defines top layer "Animation" -->
    <style name="Animation" />

    <!-- the animations must have been defined in your "anim" folder, of course -->
    <style name="Animation.ActivityAnimation">
        <!--<item name="android:activityOpenEnterAnimation">@anim/slide_in_top</item>-->
        <!--<item name="android:activityOpenExitAnimation">@anim/slide_out_bottom</item>-->
        <!--<item name="android:activityCloseEnterAnimation">@anim/slide_in_bottom</item>-->
        <!--<item name="android:activityCloseExitAnimation">@anim/slide_out_top</item>-->
        <item name="android:windowEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/slide_out_top</item>
    </style>

    <!-- Base application theme. -->
    <!--<style name="AppThemeDark" parent="Theme.AppCompat.NoActionBar">-->
    <style name="AppThemeDark" parent="@android:style/Theme.DeviceDefault.Wallpaper.NoTitleBar">
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@style/Animation.ActivityAnimation</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@style/Animation.ActivityAnimation</item>
        <item name="colorAccent">@color/black</item>
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlHighlight">@color/grey_500</item>
    </style>

</resources>
